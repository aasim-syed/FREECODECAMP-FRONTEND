const reducer = (state = 5) => {
  return state;
}

// Redux methods are available from a Redux object
const store=Redux.createStore(reducer);// For example: Redux.createStore()
// Define the store here:
